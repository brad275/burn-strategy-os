[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PYTHONPATH=src python3 -m uvicorn strategy_os.app:app --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
